import { ImgSecuredDirective } from './img-secured.directive';

describe('ImgSecuredDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgSecuredDirective();
    expect(directive).toBeTruthy();
  });
});
